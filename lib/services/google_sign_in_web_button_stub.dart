import 'package:flutter/material.dart';

/// Stub for the web-only renderButton method; only the *_web.dart variant
/// (selected via conditional export) is used on an actual web build.
Widget renderGoogleSignInButton() {
  throw StateError('renderGoogleSignInButton should only be called on web');
}
